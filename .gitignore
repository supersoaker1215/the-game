node_modules/
dist/
.vite/
.DS_Store
*.log
# Local Claude Code agent data — conversation history, cache, etc.
# Should never be committed to a public repo.
.claude/

# Local backup of full-res card art before the smart resize (kept out of deploy)
art-originals/

# Local design prototype — not shipped
proto-metal.html
proto-*.html

# Proof sheet from sim/tools/card-art-accent.py --sheet. A 2MB render of every
# painting with its proposed border around it: useful to LOOK at when the
# colours change, not something the game loads.
card-art-accent-sheet.png

# Python bytecode from sim/tools scripts
__pycache__/
*.pyc
