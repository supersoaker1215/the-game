{
  "name": "card-lane-battle",
  "version": "1.0.0",
  "private": true,
  "description": "Neon-lit lane-based card battler. 95 heroes and villains, draft mode, deckbuilder mode, AI opponent.",
  "type": "module",
  "scripts": {
    "//-static": "existing workflow — just serve the repo root over HTTP, no build needed",
    "dev:static": "python3 -m http.server 8080",
    "//-vite": "new Vite workflow — hot-reload dev + minified prod bundle",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "sim": "jsc sim/test.js || echo 'run sim/test.js under JavaScriptCore (macOS)'"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
